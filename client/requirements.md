## Packages
framer-motion | Smooth animations for wizard steps and results
lucide-react | Beautiful icons for UI elements
zod | Schema validation (already in stack but good to confirm)
clsx | Class name utility (already in stack)
tailwind-merge | Class name utility (already in stack)
react-circular-progressbar | For the score gauge visualization
canvas-confetti | Celebration effect on high scores

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Space Grotesk", "sans-serif"],
}
API endpoints defined in shared/routes.ts will be used.
